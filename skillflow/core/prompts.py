"""共用提示词模板。"""

SPEC_TO_PROMPT_TEMPLATE = """\
请根据以下技能规范（SPEC）创建一个完整的技能。

## 技能名称（必须使用）
{name}

## 技能创建位置（必须在此创建）
{output_dir}

## 技能描述
{description}

## 使用场景
{scenes}

## 输入与输出
- 输入：{input_desc}
- 输出：{output_desc}

## 必须遵守的规则
{rules}

## 示例
{examples}

{reference_section}

## 输出要求

请使用 skill-creator 技能创建完整的技能目录结构。

**关键约束**：
1. 技能必须创建在指定位置：`{output_dir}`
2. YAML frontmatter 中的 `name` 字段必须填写 `{name}`（严格使用此名称，不要自行命名）
3. 创建完成后，报告技能目录的完整路径

{lang_constraint}
"""

_SPEED_CONSTRAINTS = {
    "low": (
        "\n## 演化速度约束（low - 保守模式）\n"
        "你只能对技能做最小幅度的精准修改：\n"
        "- 仅允许修复明确的逻辑缺陷、补充缺失的关键规则\n"
        "- 禁止重组流程结构、重写段落、新增章节\n"
        "- 每次修改点控制在 1~3 个，每个修改点只改几行\n"
        "- 保持原有结构和表述不变，只改有明确问题的部分\n"
        "- 推荐使用追加方式：在现有规则末尾补充，而非修改已有内容\n"
    ),
    "medium": (
        "\n## 演化速度约束（medium - 均衡模式）\n"
        "你可以在保持整体结构的前提下做针对性改进：\n"
        "- 允许调整局部流程、补充示例、完善规则\n"
        "- 不允许对技能做结构性重构或全面重写\n"
        "- 每次修改点控制在 3~6 个，每个修改点保持局部范围\n"
        "- 推荐在现有段落内做定向修改，而非整段替换\n"
    ),
    "high": (
        "\n## 演化速度约束（high - 激进模式）\n"
        "你可以对技能做较大幅度的改进：\n"
        "- 允许流程重组、规则重写、新增章节\n"
        "- 可以大幅调整技能的结构和内容\n"
        "- 修改点数量不限，鼓励充分改进\n"
        "- 确保改动有明确的改进意图，不做无意义的润色\n"
    ),
}


def build_speed_constraint(speed: str) -> str:
    """根据 speed 枚举值生成演化约束文本。

    Args:
        speed: "low" | "medium" | "high"
    """
    return _SPEED_CONSTRAINTS.get(speed, _SPEED_CONSTRAINTS["low"])


LANG_CONSTRAINT_ZH = "重要：技能中的所有说明文字、规则描述、示例解释必须使用中文撰写。"
LANG_CONSTRAINT_EN = "Important: All instructions, rule descriptions, and example explanations in the skill must be written in English."

TEST_CASE_GEN_PROMPT = """\
你是一个测试用例生成专家。请根据以下技能描述，从技能的功能点出发生成测试用例。

## 步骤

### 第一步：识别功能点
仔细阅读技能描述，提取出该技能的独立功能点。功能点的来源包括：
- 工作流程中的各个阶段（如需求澄清、数据筛选、深度分析、报告输出）
- 技能的硬性规则和约束（如免责声明、禁止行为）
- 分析框架或参考体系中的关键维度
- 脚本/工具的调用能力

### 第二步：按功能点生成测试用例
对每个功能点生成 1-2 个测试用例。优先覆盖核心功能流程，再补充边界情况和规则约束。

## 输出格式

每个测试用例包含三个字段：
- test_point: 验证目标（一句话描述要验证哪个功能点的什么能力）
- question: 模拟用户提问（自然、具体，模拟真实用户输入。基础场景控制在 200 字以内，包含代码或数据片段的复杂场景可放宽至 500 字以内）
- checkpoints: 评估要点列表（2-4 条，每条描述好的回答中应该或不应该出现的关键要素）

### checkpoints 规则
1. 每条必须可客观判断（出现/未出现），禁止模糊表述如"应该详细""应该全面"
2. 涵盖正向要素（应该包含）和反向要素（不应该包含），至少 1 条正向
3. 只描述内容要素，不描述格式或排版
4. 每条控制在 20 字以内，简洁明确

## 要求
1. 每个功能点最多 2 个用例，不要为同一功能点生成过多重复用例
2. question 应自然具体，模拟真实用户提问，不要用"请演示XXX功能"这种测试口吻
3. 测试用例总数控制在功能点数量 × 1.5 以内
4. question 不应设计为需要用户交互确认的场景（如需要用户输入密码、二次确认、交互式选择等），应设计为一次性可完成的自包含任务

## 输出

先简要列出你识别到的功能点（用一句话），然后输出测试用例 JSON。

功能点：
- 功能点1: ...
- 功能点2: ...
...

请严格以如下 JSON 格式输出测试用例（不要包含其他内容）：
{{
  "test_cases": [
    {{
      "test_point": "验证目标",
      "question": "模拟用户提问",
      "checkpoints": ["应包含的关键要素1", "应包含的关键要素2", "不应出现的问题"]
    }},
    ...
  ]
}}

技能描述：
{skill_content}
"""

TEST_CASE_CRITIC_PROMPT = """\
你是一个测试用例质量审查专家。请对以下测试用例进行审查，找出语义重复和质量不达标的用例。

## 审查规则

### 语义重复判定
- 判定依据：test_point 的语义是否高度相似
- 如果两个用例的 test_point 验证的是同一个核心能力，即使 question 场景不同，也算重复
- 重复时保留 question 更具体、更有代表性的用例，移除另一个

### 质量判定
- question 是否使用了测试口吻（如"请演示XX功能""请测试XX"）
- question 是否过于笼统、缺乏具体上下文
- checkpoint 是否包含模糊表述（如"应该详细""应该全面""应该清晰"）
- checkpoint 是否只描述格式/排版而非内容要素
- test_point 是否描述了明确的验证目标

## 待审查的测试用例

{test_cases_json}

## 输出要求

请严格以如下 JSON 格式输出审查结果（不要包含其他内容）：
{{
  "actions": [
    {{"index": 用例编号(从0开始), "action": "remove", "reason": "重复/质量问题的原因"}},
    {{"index": 用例编号(从0开始), "action": "regenerate", "reason": "需要重新生成的原因"}},
    {{"action": "supplement", "function_point": "遗漏的功能点描述", "reason": "为什么需要补充"}}
  ]
}}

注意：
- 没有问题的用例不要出现在 actions 中，它们会被保留
- action 为 remove 时表示该用例应被删除
- action 为 regenerate 时表示该用例需要重写
- action 为 supplement 时表示需要新增用例补充遗漏的功能点（不含 index 字段）
- 如果所有用例都没有问题，输出 {{"actions": []}}
"""

TEST_CASE_REVISE_PROMPT = """\
请根据审查意见对测试用例进行修订。

## 当前测试用例

{test_cases_json}

## 审查意见

{critic_actions}

## 技能描述（供参考）

{skill_content}

## 修订要求

1. 对于标记为 remove 的用例：直接删除
2. 对于标记为 regenerate 的用例：根据 reason 重写该用例，保持相同的验证目标但改善质量
3. 对于标记为 supplement 的条目：针对遗漏的功能点生成新的测试用例
4. 没有被标记的用例原样保留，不做任何修改
5. 确保修订后所有用例的 checkpoints 可客观判断，不含模糊表述

## 输出

请严格以如下 JSON 格式输出修订后的完整测试用例列表（不要包含其他内容）：
{{
  "test_cases": [
    {{
      "test_point": "验证目标",
      "question": "模拟用户提问（基础场景200字以内，含代码/数据的复杂场景500字以内）",
      "checkpoints": ["应包含的关键要素1", "应包含的关键要素2"]
    }},
    ...
  ]
}}
"""

JUDGE_PROMPT = """\
你是一个严格苛刻的评估裁判。请以批判性眼光逐项审视技能响应，找出所有不足之处。

## 测试目标
{test_point}

## 用户提问
{question}

## 评估要点（逐条检查）
{checkpoints}

## 技能响应
{response}
{trace_section}
### 审视维度（逐项检查，发现问题即扣分）

| # | 维度 | 审视要点 | 扣分范围 |
|---|------|---------|---------|
| 1 | 答非所问 | 未精准理解用户核心诉求，答复与问题无关，无实际参考价值 | 轻度偏移 -0.1~0.2，严重跑题 -0.3~0.5 |
| 2 | 事实失真 / AI 幻觉 | 存在常识错误、捏造数据、虚构文献资料等不实内容 | 每处 -0.15~0.25 |
| 3 | 关键信息缺失 | 遗漏解题核心要素、关键步骤与必要变量 | 轻度遗漏 -0.1，核心缺失 -0.2~0.3 |
| 4 | 内容逻辑杂乱 | 结构混乱、冗余重复、废话堆砌，逻辑断裂，排版缺乏条理与美观度 | 轻度 -0.05~0.1，严重 -0.15~0.2 |
| 5 | 表述用语不当 | 措辞生硬欠妥，沟通态度傲慢，存在明显表达问题 | 每处 -0.05~0.1 |
| 6 | 擅自预判意图 | 未提前澄清，主观臆断用户真实需求 | -0.1~0.2 |
| 7 | 内容存在风险 | 突破合规边界，未标注必要提示、风险警示与安全说明 | -0.1~0.3 |
| 8 | 评估要点未覆盖 | 未满足上方"评估要点"中列出的关键要素，逐条检查每条 checkpoint 是否在响应中得到体现 | 每遗漏一条 -0.1~0.2 |
| 9 | 过程质量 | 参考执行过程评估路径合理性：工具调用是否出现失败、是否存在无意义的重复操作。需结合任务复杂度综合判断，复杂任务允许更多步骤。无执行过程数据时不扣分。若技能未被触发，意味着 agent 未加载该技能的任何指令和规则，当前响应反映的是基础模型能力而非技能质量，应扣 -0.2 | 轻度问题 -0.05~0.1，严重低效 -0.1~0.2 |

### 评分规则

- 基础分 = 1.0
- score = 基础分 - 各维度累计扣分（下限 0.0）
- 逐项严格检查每个维度，只有确实存在问题时才扣分，不要无中生有
- 同一维度内存在多处问题，按最严重等级扣分
- 第 8 维须逐条检查每条 checkpoint，正向 checkpoint 未覆盖则扣分，反向 checkpoint 出现则扣分

### pass 判定标准
- pass=true：score >= 0.8
- pass=false：score < 0.8

### 评分校准参考
- 0.9-1.0：优秀，无可挑剔
- 0.8-0.89：良好，存在轻微不足但不影响核心质量
- 0.6-0.79：合格，有明显不足但核心目标基本达成
- 0.4-0.59：欠佳，存在多处问题，核心目标部分达成
- 0.0-0.39：不合格，核心目标未达成或存在严重问题

请严格以如下 JSON 格式输出（不要包含其他内容）：
{{
  "pass": true/false,
  "score": 0.0-1.0,
  "reason": "逐项审视：1.答非所问=_(扣_分) 2.事实失真=_(扣_分) 3.关键信息缺失=_(扣_分) 4.内容逻辑=_(扣_分) 5.表述用语=_(扣_分) 6.擅自预判=_(扣_分) 7.内容风险=_(扣_分) 8.要点覆盖=_(扣_分) 9.过程质量=_(扣_分)；累计扣分=_；最终score=_"
}}
"""

JUDGE_PROMPT_WITH_REFERENCE = """\
你是一个严格苛刻的评估裁判。请以批判性眼光逐项审视技能响应，找出所有不足之处。

## 测试目标
{test_point}

## 用户提问
{question}

## 评估要点（逐条检查）
{checkpoints}

## 参考答案（人工提供的高质量标准答案，作为事实对照基准）
{expected}

## 技能响应
{response}
{trace_section}
### 审视维度（逐项检查，发现问题即扣分）

| # | 维度 | 审视要点 | 扣分范围 |
|---|------|---------|---------|
| 1 | 答非所问 | 未精准理解用户核心诉求，答复与问题无关，无实际参考价值 | 轻度偏移 -0.1~0.2，严重跑题 -0.3~0.5 |
| 2 | 事实失真 / AI 幻觉 | 存在常识错误、捏造数据、虚构文献资料等不实内容 | 每处 -0.15~0.25 |
| 3 | 关键信息缺失 | 遗漏解题核心要素、关键步骤与必要变量 | 轻度遗漏 -0.1，核心缺失 -0.2~0.3 |
| 4 | 内容逻辑杂乱 | 结构混乱、冗余重复、废话堆砌，逻辑断裂，排版缺乏条理与美观度 | 轻度 -0.05~0.1，严重 -0.15~0.2 |
| 5 | 表述用语不当 | 措辞生硬欠妥，沟通态度傲慢，存在明显表达问题 | 每处 -0.05~0.1 |
| 6 | 擅自预判意图 | 未提前澄清，主观臆断用户真实需求 | -0.1~0.2 |
| 7 | 内容存在风险 | 突破合规边界，未标注必要提示、风险警示与安全说明 | -0.1~0.3 |
| 8 | 评估要点未覆盖 | 未满足上方"评估要点"中列出的关键要素，逐条检查每条 checkpoint 是否在响应中得到体现 | 每遗漏一条 -0.1~0.2 |
| 9 | 参考答案不一致 | 对照参考答案，检查技能响应中的事实性陈述、关键数据、核心结论是否与参考答案一致。重点关注：数据/数值是否吻合、流程步骤是否遗漏、核心结论是否矛盾 | 轻度偏差 -0.1~0.2，核心事实矛盾 -0.2~0.4 |
| 10 | 过程质量 | 参考执行过程评估路径合理性：工具调用是否出现失败、是否存在无意义的重复操作。需结合任务复杂度综合判断，复杂任务允许更多步骤。无执行过程数据时不扣分。若技能未被触发，意味着 agent 未加载该技能的任何指令和规则，当前响应反映的是基础模型能力而非技能质量，应扣 -0.2 | 轻度问题 -0.05~0.1，严重低效 -0.1~0.2 |

### 评分规则

- 基础分 = 1.0
- score = 基础分 - 各维度累计扣分（下限 0.0）
- 逐项严格检查每个维度，只有确实存在问题时才扣分，不要无中生有
- 同一维度内存在多处问题，按最严重等级扣分
- 第 8 维须逐条检查每条 checkpoint，正向 checkpoint 未覆盖则扣分，反向 checkpoint 出现则扣分
- 第 9 维以参考答案为事实对照基准，响应中的事实性内容应与参考答案一致，但不要求表述完全相同

### pass 判定标准
- pass=true：score >= 0.8
- pass=false：score < 0.8

### 评分校准参考
- 0.9-1.0：优秀，无可挑剔
- 0.8-0.89：良好，存在轻微不足但不影响核心质量
- 0.6-0.79：合格，有明显不足但核心目标基本达成
- 0.4-0.59：欠佳，存在多处问题，核心目标部分达成
- 0.0-0.39：不合格，核心目标未达成或存在严重问题

请严格以如下 JSON 格式输出（不要包含其他内容）：
{{
  "pass": true/false,
  "score": 0.0-1.0,
  "reason": "逐项审视：1.答非所问=_(扣_分) 2.事实失真=_(扣_分) 3.关键信息缺失=_(扣_分) 4.内容逻辑=_(扣_分) 5.表述用语=_(扣_分) 6.擅自预判=_(扣_分) 7.内容风险=_(扣_分) 8.要点覆盖=_(扣_分) 9.参考答案一致性=_(扣_分) 10.过程质量=_(扣_分)；累计扣分=_；最终score=_"
}}
"""

EVOLVE_REFLECT_PROMPT = """\
你是一个技能质量分析专家。请分析以下技能在测试中的表现。

## 当前技能内容（SKILL.md）
{skill_content}

## 失败的测试用例
{failed_cases}

## 失败原因分析（来自评估裁判）
{failure_reasons}

## 高方差范例（同一用例中高分与低分 trial 的对比启发）
{exemplars_section}

{trace_context}

## 重要约束：抽象而非记忆

你看到的是具体的测试场景和响应，但你的分析必须提炼为**通用的能力缺陷描述**。
- 禁止在分析结论中复述或引用具体的测试问题原文
- 禁止针对特定测试场景提出改进建议（如"当用户问XXX时应答YYY"）
- 禁止提议在技能中加入能匹配特定测试输入的规则或示例
- 正确做法：从具体失败中归纳出通用的能力不足，提出适用于**所有同类场景**的改进方案

{speed_constraint}

请对每个失败用例做双面分析：

### A. 正面价值提取
即便该用例未通过，也请评估：
- 回应路径是否简洁高效？（比如是否用最少的步骤达成了目标）
- 回应中哪些部分质量很高，值得作为范例保留？
- 回应中是否存在可以直接写入技能的通用优质模式/结构？

### B. 不足分析与改进
- 指令是否清晰、完整
- 规则是否充分覆盖了该类失败场景
- 示例是否具有代表性和通用性
- 是否缺少关键的处理流程或边界条件说明

请先逐个用例分析，然后给出综合改进建议（纯文本，不要 JSON）：
"""

EVOLVE_REWRITE_PROMPT = """\
请根据反思分析，对 SKILL.md 做精准的定向修改，使其在失败场景下表现更好。

## 当前 SKILL.md
{skill_content}

## 反思分析
{reflection}

## 修改要求
1. 保持 YAML frontmatter 的 name 和 description 字段不变
2. 针对反思中指出的通用能力不足进行精准改进
3. 可以增加规则、完善流程、补充通用示例
4. 不要删除已有的正确内容
5. 保持技能的简洁性，不要过度膨胀
{exemplars_instruction}

## 重要约束
- 禁止输出完整的 SKILL.md，只输出需要修改的片段
- 禁止在修改中引用、暗示或包含任何具体测试问题的内容
- 每个修改必须解决一个通用的能力缺陷，而非特定测试场景
- 新增的示例必须是通用场景，不能是测试用例的变体

## 输出格式

先简要说明你要做的修改及理由（1-3 句话），然后只输出需要修改的片段：

<<<FILE:SKILL.md>>>
<<<<<<< SEARCH
原文中要被替换的片段（必须与原文件完全一致，包括缩进和空行）
=======
替换后的新片段
>>>>>>> REPLACE
<<<END>>>

注意：
- SEARCH 部分必须与原文件完全一致（逐字符匹配），否则替换会失败
- 可以有多个 SEARCH/REPLACE 块，按从上到下的顺序排列
- 如果反思分析中没有值得修改的地方，只输出说明即可，不要做无意义修改
"""

EVOLVE_AUDIT_PROMPT = """\
你是一个技能质量审视专家。请按照以下技能演化优化标准对技能进行全面审视，找出需要改进的地方并输出改进后的文件。

## 技能文件

{skill_files}

{trace_context}

{past_strategies}
## 技能演化优化策略

一、 定义与边界 (Definition & Scope)
1. 全量演化覆盖：明确演化范围涵盖技能的核心组件，包括但不限于主控逻辑（SKILL.md）、核心脚本（scripts/）及参考文档（references/）。除静态资源（assets/）与原始知识库外，所有影响功能的逻辑文件均可以参与演化。
2. 职责边界清晰：明确技能的功能范畴，界定"能做"与"不能做"的边界，确保预期可控。
3. 触发场景精准：优化触发逻辑，确保在目标场景下稳定唤醒，最大程度降低误触发率。
4. 行为表里如一：技能的实际运行逻辑须与 description 描述高度一致，严禁出现超出说明范围的隐匿行为。

二、 架构与工程规范 (Architecture & Engineering)
1. 输入输出标准化：针对工程类场景，可以优先考虑使用 JSON/YAML 等结构化协议，确保数据交互的鲁棒性。
2. 依赖关系闭环：确保技能提到的第三方知识库、代码或 API 引用均有明确的访问方式或调用路径，消除配置缺失。
3. 目录结构合理化：
    * scripts/：存放确定性的、重复性的任务用脚本。
    * references/：解耦长篇文档，在主控文件（SKILL.md）中精准标注调用时机。
    * assets/：统一管理模板、图标等静态资源。
    * 大文件加目录：引用文件超过 300 行时，开头加目录（Table of Contents），建立目录索引（ToC），提升上下文检索效率
    * 多领域按变体组织：涉及多框架时，采用"主控逻辑 + 插件化细节"模式，SKILL.md 只放流程+选择逻辑，各框架细节拆到references/ 子目录

三、 逻辑与性能 (Logic & Performance)
1. 代码质量交付：确保 scripts/ 下的脚本高效、无偏、无 Bug，并经过严苛的边界条件校验。
2. 实现方案极简：在同等效果下，优先采纳表述更简洁、流程更精简的实现路径。
3. 配置权限最小化：精简 YAML 头信息，严格遵循"最小权限原则"，确保权限集刚好覆盖业务需求。

四、 稳定性与安全 (Stability & Security)
1. 异常容错机制：完善边界条件与错误处理逻辑，严防死循环及异常资源损耗，确保系统高可用。
2. 测试覆盖全量化：构建完备的测试用例库，确保功能点与业务链路的覆盖率达标。
3. 合规与安全性：严禁集成恶意软件、漏洞利用或数据窃取逻辑；交互措辞需温和得体，符合职业化标准。

## 输出要求

先输出你的审视分析，逐条标准评估每个文件的当前状态，给出具体问题和改进建议。

然后在分析之后，只输出需要修改的片段。格式如下：

<<<FILE:相对路径>>>
<<<<<<< SEARCH
原文中要被替换的片段（必须与原文件完全一致，包括缩进和空行）
=======
替换后的新片段
>>>>>>> REPLACE
<<<END>>>

注意：
- 相对路径必须在同一行内，如 SKILL.md、scripts/helper.py、references/guide.md
- 每个修改只输出被替换的片段，不要输出整个文件。未修改的部分不要输出
- SEARCH 部分必须与原文件完全一致（逐字符匹配），否则替换会失败
- 一个文件可以有多个 SEARCH/REPLACE 块，按从上到下的顺序排列
- <<<END>>> 必须单独一行，与 <<<FILE:>>> 严格配对
- 如果所有文件都不需要修改，只输出审视分析即可

{speed_constraint}
## 演化约束（必须严格遵守）

1. 禁止无实质意义的改动，包括但不限于：措辞润色、排版调整、注释修改、同义词替换、标点修正。这类改动不会改善评估结果，只会浪费演化轮次。
2. 每次改动必须针对具体的功能缺陷或逻辑问题，改动应集中在：规则增补、流程修正、分支逻辑补充、示例替换。
3. 如果你认为当前技能已无改进空间，请不要修改任何文件，只输出审视分析即可。
4. 参考"已尝试演化记录"中的历史方向和效果，自主决策演化策略：可以在有效方向上继续深化，也可以组合多个有效方向，或尝试全新的改进角度。
"""

EXEMPLAR_EXTRACT_PROMPT = """\
你是一个技能优化专家。以下是一个未通过评估但包含高质量元素的回应。

## 用户提问
{question}

## 测试目标
{test_point}

## 评估要点
{checkpoints}

## 技能回应（得分 {score}）
{response}

## 评估反馈
{reason}

请提取这个回应中的优质模式，转化为可直接写入技能的示例片段。重点关注回应中哪些评估要点覆盖得较好。

要求：
1. 只提取真正有价值的部分（简洁的流程、清晰的结构、高效的做法）
2. 转化为 skill 中的 example 格式
3. 如果回应没有值得提取的内容，输出 EMPTY

输出格式：
- 模式描述：一句话描述这个模式为什么好
- 示例片段：可以直接插入 SKILL.md 的内容
"""

EXEMPLAR_CONTRAST_PROMPT = """\
你是一个技能优化专家。以下是一个测试用例的多次执行结果对比，最高分和最低分之间存在显著差异。
请分析差异原因，提取最高分试验中的成功模式。

## 用户提问
{question}

## 测试目标
{test_point}

## 评估要点
{checkpoints}

## 最高分响应（得分 {best_score}）
{best_response}

## 最高分评估反馈
{best_reason}

## 最低分响应（得分 {worst_score}）
{worst_response}

## 最低分评估反馈
{worst_reason}

{trace_section}
请对比两次响应，分析以下问题：

1. **关键差异**：最高分响应在哪些具体方面优于最低分响应？
   - 评估要点覆盖的差异
   - 内容准确性和完整性的差异
   - 结构和逻辑的差异

2. **成功模式**：最高分响应中哪些做法值得固化为技能规则或示例？
   - 具体的处理步骤或流程
   - 关键信息的选择和呈现方式
   - 边界条件的处理方式

3. **失败原因**：最低分响应具体在哪些点走偏了？

## 重要约束
- 你看到的是具体测试场景，但必须提炼出**通用的行为模式**
- 输出的规则片段必须适用于所有同类场景，而非仅对当前测试输入有效
- 禁止在规则片段中引用、暗示或包含任何具体测试场景的内容
- 如果提取的模式只适用于该具体场景而非同类场景，输出 EMPTY

请输出可直接用于改进技能的结论：

输出格式：
- 诊断：一句话概括两次响应的核心差异
- 模式描述：描述最高分响应的成功模式，说明为什么有效（必须是通用描述）
- 规则片段：可以直接插入 SKILL.md 的通用规则片段（禁止包含任何具体场景细节）

如果两次响应没有有意义的差异，输出 EMPTY
"""

CHANGE_JUDGE_PROMPT = """\
以下是技能文件的变化（统一差异格式）：

{diff_content}

请判断这些变化是否属于有意义的改进（如修改了关键规则、调整了核心流程、修复了逻辑错误），还是仅属于表面调整（如措辞润色、排版调整、注释修改）。

请严格以如下 JSON 格式输出（不要包含其他内容）：
{{
  "meaningful": true/false,
  "reason": "一句话说明判断依据"
}}
"""
