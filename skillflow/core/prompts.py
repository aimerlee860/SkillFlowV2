"""共用提示词模板。"""

SPEC_TO_PROMPT_TEMPLATE = """\
请根据以下技能规范（SPEC）创建一个完整的技能。

## 技能描述
{description}

## 使用场景
{scenes}

## 输入与输出
- 输入：{input_desc}
- 输出：{output_desc}

## 必须遵守的规则
{rules}

## 示例
{examples}

{reference_section}

请按照 deepagents 技能格式，生成一个完整的 SKILL.md 文件。包含 YAML frontmatter（name, description）和 markdown body。
{lang_constraint}
"""

LANG_CONSTRAINT_ZH = "重要：技能中的所有说明文字、规则描述、示例解释必须使用中文撰写。"
LANG_CONSTRAINT_EN = "Important: All instructions, rule descriptions, and example explanations in the skill must be written in English."

TEST_CASE_GEN_PROMPT = """\
你是一个测试用例生成专家。请根据以下技能描述，从技能的功能点出发生成测试用例。

## 步骤

### 第一步：识别功能点
仔细阅读技能描述，提取出该技能的独立功能点。功能点的来源包括：
- 工作流程中的各个阶段（如需求澄清、数据筛选、深度分析、报告输出）
- 技能的硬性规则和约束（如免责声明、禁止行为）
- 分析框架或参考体系中的关键维度
- 脚本/工具的调用能力

### 第二步：按功能点生成测试用例
对每个功能点生成 1-2 个测试用例。优先覆盖核心功能流程，再补充边界情况和规则约束。

## 输出格式

每个测试用例包含两个字段：
- test_point: 验证目标（一句话描述要验证哪个功能点的什么能力）
- question: 模拟用户提问（自然、具体，模拟真实用户输入）

## 要求
1. 每个功能点最多 2 个用例，不要为同一功能点生成过多重复用例
2. question 应自然具体，模拟真实用户提问，不要用"请演示XXX功能"这种测试口吻
3. 测试用例总数控制在功能点数量 × 1.5 以内

## 输出

先简要列出你识别到的功能点（用一句话），然后输出测试用例 JSON。

功能点：
- 功能点1: ...
- 功能点2: ...
...

请严格以如下 JSON 格式输出测试用例（不要包含其他内容）：
{{
  "test_cases": [
    {{"test_point": "...", "question": "..."}},
    ...
  ]
}}

技能描述：
{skill_content}
"""

JUDGE_PROMPT = """\
你是一个客观公正的评估裁判。请基于以下测试目标，对技能响应进行评估。

## 测试目标
{test_point}

## 用户提问
{question}

## 技能响应
{response}

### 评分维度（每项 0-1 分，加权汇总）

| 维度 | 权重 | 评分标准 |
|------|------|---------|
| 核心目标达成 | 40% | 响应是否达成了测试目标的核心要求？完全达成=1.0，大部分达成=0.7-0.9，部分达成=0.4-0.6，几乎未达成=0.0-0.3 |
| 完整性 | 25% | 是否覆盖了测试目标涉及的各个要点？全覆盖=1.0，覆盖主要部分=0.6-0.9，有明显遗漏=0.3-0.5 |
| 实用性 | 20% | 响应内容是否有具体价值？提供了可操作的具体信息=1.0，有部分实用内容=0.5-0.9，基本是泛泛之谈=0.0-0.4 |
| 相关性 | 15% | 是否紧扣用户提问？紧扣=1.0，偶有偏移但整体相关=0.6-0.9，明显跑题=0.0-0.5 |

### 扣分细则（从加权汇总分中扣减）

| 问题类型 | 扣分 |
|---------|------|
| 事实性错误（编造数据、错误论断） | -0.15/处 |
| 明确遗漏关键信息 | -0.1/处 |
| 未经澄清就假设用户意图 | -0.05~0.1 |
| 结构混乱、冗长重复 | -0.05 |
| 缺少边界/风险提示（仅当测试目标明确要求时） | -0.05 |

**注意**：
- 小瑕疵（措辞不完美、格式微调）不扣分
- 内容整体方向正确但因对话轮次限制未展开，不视为遗漏
- 不要对单次响应求全责备，关注核心目标是否达成

### score 计算公式

score = 维度加权汇总 - 累计扣分（下限为 0.0）

### pass 判定标准
- pass=true：score >= 0.8
- pass=false：score < 0.8

### 评分校准参考
- 0.9-1.0：优秀，核心目标完全达成，无明显缺陷
- 0.8-0.89：良好，核心目标达成，有小瑕疵但不影响整体质量
- 0.6-0.79：合格，核心目标基本达成但有明显不足
- 0.4-0.59：欠佳，核心目标部分达成
- 0.0-0.39：不合格，核心目标未达成

请严格以如下 JSON 格式输出（不要包含其他内容）：
{{
  "pass": true/false,
  "score": 0.0-1.0,
  "reason": "逐维度评分：核心目标=_, 完整性=_, 实用性=_, 相关性=_；扣分项：_；最终 score=加权汇总-扣分=_"
}}
"""

EVOLVE_REFLECT_PROMPT = """\
你是一个技能质量分析专家。请分析以下技能在测试中的表现，找出不足之处。

## 当前技能内容（SKILL.md）
{skill_content}

## 失败的测试用例
{failed_cases}

## 失败原因分析（来自评估裁判）
{failure_reasons}

请从以下几个方面分析技能的不足：
1. 指令是否清晰、完整
2. 规则是否充分覆盖了失败场景
3. 示例是否具有代表性
4. 是否缺少关键的处理流程或边界条件说明

请输出你的反思分析（纯文本，不要 JSON）：
"""

EVOLVE_REWRITE_PROMPT = """\
请根据反思分析，改写以下技能的 SKILL.md 文件，使其在失败场景下表现更好。

## 当前 SKILL.md
{skill_content}

## 反思分析
{reflection}

## 改写要求
1. 保持 YAML frontmatter 的 name 和 description 字段
2. 针对反思中指出的不足进行改进
3. 可以增加规则、完善流程、补充示例
4. 不要删除已有的正确内容
5. 保持技能的简洁性，不要过度膨胀

请直接输出改写后的完整 SKILL.md 内容（包含 YAML frontmatter）：
"""
